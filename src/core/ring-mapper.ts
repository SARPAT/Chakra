// Ring Mapper — Route lookup table
// Compiles developer annotations into flat O(1) lookup: METHOD:PATH → RouteInfo
// Holds the Level Map (which blocks are active/suspended at each level 0-3)

export {};
